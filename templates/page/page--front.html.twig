{#
/**
 * @file
 * Dungeon Crawler Life - Front Page
 * Clean, focused home page
 */
#}

{{ attach_library('dungeoncrawler/animated-header') }}

<div class="page-wrapper">
  {# Hero Section #}
  <div id="large-header" class="large-header home-header">
    <canvas id="demo-canvas"></canvas>
    <h1 class="main-title">
      <span class="highlight">Dungeon Crawler</span>
      <span class="thin">Life</span>
    </h1>
    <p class="main-subtitle">
      An AI-powered living dungeon where skill checks happen automatically and sharp characters uncover more
    </p>
    <div class="hero-cta">
      <a href="{{ primary_cta_url|default('/campaigns') }}" class="btn btn-primary">{{ primary_cta_label|default('Start Your Adventure') }}</a>
      <a href="{{ secondary_cta_url|default('/how-to-play') }}" class="btn btn-outline-light">{{ secondary_cta_label|default('Learn More') }}</a>
    </div>
  </div>

  {# Navigation #}
  {% block page_navigation %}
    {% embed 'radix:navbar' with {
      navbar_container_type: null,
      navbar_expand: 'lg',
      navbar_theme: 'dark',
      navbar_utility_classes: ['dungeoncrawler-navbar'],
    } %}
      {% block branding %}
        {% if page.navbar_branding %}
          {{ page.navbar_branding }}
        {% endif %}
      {% endblock %}
      {% block left %}
        {% if page.navbar_left %}
          <div class="me-auto">
            {{ page.navbar_left }}
          </div>
        {% endif %}
      {% endblock %}
      {% block right %}
        {% if page.navbar_right %}
          {{ attach_library('dungeoncrawler/navbar_right') }}
          <div class="ms-auto region-navbar-right">
            {{ page.navbar_right }}
          </div>
        {% endif %}
      {% endblock %}
    {% endembed %}
  {% endblock %}

  {# Main Content #}
  <main class="main-content">
    <div class="container">

      {# Community feedback notice #}
      <section class="mb-4">
        <div class="alert alert-info text-center" role="status">
          We are actively implementing player suggestions. Keep the ideas coming!
        </div>
      </section>

      {# Key Features - Simple 3-Column #}
      <section class="features-section">
        <div class="features-grid">
          <div class="feature-card feature-card--ai-generated">
            <h3>AI-Generated</h3>
            <p>Every room, monster, and encounter is created by AI the moment you discover it, then expanded as your party uncovers more</p>
          </div>
          <div class="feature-card feature-card--permanent-world">
            <h3>Checks Matter</h3>
            <p>Perception and other skill checks are rolled automatically, unlocking extra room details, hidden clues, and richer exploration</p>
          </div>
          <div class="feature-card feature-card--party-play">
            <h3>Party Play</h3>
            <p>Team up with other adventurers and let every specialist contribute as the dungeon reacts to what your party can actually notice</p>
          </div>
        </div>
      </section>

      {# Quick Stats #}
      <section class="stats-section">
        <div class="stats-row">
          <div class="stat-item">
            <span class="stat-number">∞</span>
            <span class="stat-label">Rooms</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">16</span>
            <span class="stat-label">Classes</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">11</span>
            <span class="stat-label">Ancestries</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">AI</span>
            <span class="stat-label">Powered</span>
          </div>
        </div>
      </section>

      {# Final CTA #}
      <section class="cta-section">
        <div class="cta-content cta-content--explore">
          <h2>Ready to <span class="highlight">Explore</span>?</h2>
          <p>Create your campaign, build a party with real strengths, and watch successful checks unlock more of the dungeon</p>
          <a href="{{ primary_cta_url|default('/campaigns') }}" class="btn btn-primary">{{ primary_cta_label|default('Create Your Campaign') }}</a>
        </div>
      </section>

    </div>
  </main>

  {# Footer #}
  {% if page.footer %}
    {{ page.footer }}
  {% endif %}
</div>
